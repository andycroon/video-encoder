---
phase: 03-pipeline-runner
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/encoder/pipeline.py
autonomous: true
requirements:
  - PIPE-05
  - PIPE-06
  - PIPE-07
  - CONF-01
  - CONF-02
  - CONF-04

must_haves:
  truths:
    - "test_x264_encode passes: lavfi chunk encodes to x264 .mp4 > 0 bytes"
    - "test_vmaf_score passes: encoded chunk scored against FFV1 source returns float in [0, 100]"
    - "test_crf_feedback_loop passes: wide target range (vmaf_min=50, vmaf_max=100) converges in 1 iteration"
    - "test_crf_oscillation_guard passes: alternating mock VMAF terminates without infinite loop"
    - "test_x264_params_str passes: dict serializes to colon-separated key=value string"
  artifacts:
    - path: "src/encoder/pipeline.py"
      provides: "Implemented: _encode_chunk_x264, _vmaf_score, _encode_chunk_with_vmaf, _x264_params_str; CRF loop with oscillation guard and cancel polling"
      exports: ["run_pipeline", "PipelineError", "DEFAULT_CONFIG"]
  key_links:
    - from: "src/encoder/pipeline.py (_vmaf_score)"
      to: "assets/vmaf_v0.6.1.json"
      via: "VMAF_MODEL constant = Path(__file__).parent.parent.parent / 'assets' / 'vmaf_v0.6.1.json'"
      pattern: "vmaf_v0\\.6\\.1\\.json"
    - from: "src/encoder/pipeline.py (_vmaf_score)"
      to: "src/encoder/ffmpeg.py (escape_vmaf_path)"
      via: "escape_vmaf_path(vmaf_model) and escape_vmaf_path(log_path)"
      pattern: "escape_vmaf_path"
    - from: "src/encoder/pipeline.py (_encode_chunk_with_vmaf)"
      to: "src/encoder/db.py (update_chunk, append_job_log)"
      via: "await update_chunk(..., vmaf_score=..., crf_used=...) after each loop iteration"
      pattern: "update_chunk"
---

<objective>
Implement pipeline steps 5-7: per-chunk x264 encode, VMAF scoring, and the CRF feedback loop with oscillation guard. Turn 5 RED tests GREEN.

Purpose: This is the core quality control logic — the VMAF CRF feedback loop that makes the encoder hit a precise quality target. The most complex and risk-prone part of the pipeline.
Output: _encode_chunk_x264, _vmaf_score, _encode_chunk_with_vmaf, _x264_params_str fully implemented.
</objective>

<execution_context>
@C:/Users/owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-pipeline-runner/03-CONTEXT.md
@.planning/phases/03-pipeline-runner/03-RESEARCH.md
@.planning/phases/03-pipeline-runner/03-02-SUMMARY.md

<interfaces>
<!-- escape_vmaf_path from src/encoder/ffmpeg.py — REQUIRED for VMAF filter strings -->
```python
from encoder.ffmpeg import run_ffmpeg, escape_vmaf_path, FfmpegError
# escape_vmaf_path handles Windows C:\\ colon triple-escaping for libvmaf filter
```

<!-- DB functions needed in the CRF loop -->
```python
async def update_chunk(db_path, *, chunk_id, vmaf_score=None, crf_used=None, status=None) -> None
async def append_job_log(db_path, job_id, text) -> None
```

<!-- VMAF filter graph (CRITICAL — see RESEARCH.md Pitfall 1) -->
# Input 0 = encoded (distorted), Input 1 = FFV1 source (reference)
# format=yuv420p on BOTH inputs prevents silent VMAF=0 (pixel format mismatch)
# setpts=PTS-STARTPTS on BOTH inputs prevents frame misalignment
filter_graph = (
    "[0:v]setpts=PTS-STARTPTS,format=yuv420p[dist];"
    "[1:v]setpts=PTS-STARTPTS,format=yuv420p[ref];"
    "[dist][ref]libvmaf=model='path={escaped_model}'"
    ":log_fmt=json:log_path={escaped_log}:n_threads=4"
)
cmd = [ffmpeg, "-y", "-i", encoded_chunk, "-i", ffv1_chunk, "-lavfi", filter_graph, "-f", "null", "-"]

<!-- VMAF JSON log parsing (pooled_metrics format from libvmaf 2.x) -->
# data["pooled_metrics"]["vmaf"]["mean"] → float score
# Fallback: average data["frames"][i]["metrics"]["vmaf"] per frame
# Fallback: regex "VMAF score: X.XX" in proc.stderr_lines
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _x264_params_str and _encode_chunk_x264</name>
  <files>src/encoder/pipeline.py</files>
  <behavior>
    - _x264_params_str(params: dict) -> str: joins dict items as "k=v" pairs with ":" separator; order preserved (Python 3.7+ dicts are ordered); empty dict returns ""
    - _encode_chunk_x264(ffmpeg, chunk_in, chunk_out, crf, x264_params): builds command with -c:v libx264 -crf {crf} -x264-params {_x264_params_str(x264_params)} -an; drains run_ffmpeg; raises PipelineError on FfmpegError
    - test_x264_params_str: PASSES — {"trellis": "2", "subq": "10"} → "trellis=2:subq=10"
    - test_x264_encode: PASSES — lavfi testsrc2 1s 320x240 chunk encodes to .mp4 > 0 bytes with DEFAULT_CONFIG x264_params at CRF 17
  </behavior>
  <action>
    Implement _x264_params_str(params: dict) -> str at module level (pure function, no async):
    ```python
    def _x264_params_str(params: dict) -> str:
        if not params:
            return ""
        return ":".join(f"{k}={v}" for k, v in params.items())
    ```

    Implement _encode_chunk_x264 as async:
    ```python
    async def _encode_chunk_x264(ffmpeg, chunk_in, chunk_out, crf, x264_params):
        cmd = [ffmpeg, "-y", "-i", str(chunk_in),
               "-c:v", "libx264", "-crf", str(crf),
               "-x264-params", _x264_params_str(x264_params),
               "-an", str(chunk_out)]
        proc = run_ffmpeg(cmd)
        for _ in proc:
            pass  # drain
    ```
    Wrap in try/except FfmpegError → raise PipelineError.
  </action>
  <verify>
    <automated>cd /c/VibeCoding/video-encoder && python -m pytest tests/test_pipeline.py::test_x264_params_str tests/test_pipeline.py::test_x264_encode -x -v 2>&1 | tail -10</automated>
  </verify>
  <done>2 tests GREEN: test_x264_params_str, test_x264_encode</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _vmaf_score and _encode_chunk_with_vmaf (CRF loop)</name>
  <files>src/encoder/pipeline.py</files>
  <behavior>
    - _vmaf_score(ffmpeg, encoded_chunk, ffv1_chunk, vmaf_model): writes VMAF JSON log to tempfile; builds filter graph with setpts+format on both inputs; runs ffmpeg; parses pooled_metrics.vmaf.mean from JSON (with fallback to frames average, then stderr regex); cleans up temp file; raises PipelineError if score is None
    - _encode_chunk_with_vmaf(ffmpeg, chunk_in, chunk_out, ffv1_chunk, vmaf_model, config, cancel_event, db_path, job_id, chunk_id, chunk_label): implements CRF loop; tracks visited_crfs set; max 10 iterations; breaks on: score in range, CRF revisited (oscillation), CRF bounds hit; appends each iteration to DB log; prints "[{chunk_label}] CRF {crf} -> VMAF {vmaf:.2f} (pass|bounds)"; awaits update_chunk with final vmaf_score and crf_used; returns (final_crf, final_vmaf, iterations)
    - test_vmaf_score: PASSES — encoded chunk vs FFV1 source (same lavfi content, low CRF) returns float in [0, 100]
    - test_crf_feedback_loop: PASSES — wide range config (vmaf_min=50, vmaf_max=100) converges immediately, returns tuple (crf, vmaf, 1)
    - test_crf_oscillation_guard: PASSES — monkeypatched _vmaf_score that alternates 95.0/98.0 terminates within 10 iterations without raising
  </behavior>
  <action>
    Implement _vmaf_score as async:
    - Use `tempfile.NamedTemporaryFile(suffix=".json", delete=False)` to get log_path
    - Call `escape_vmaf_path(vmaf_model)` and `escape_vmaf_path(log_path)` for filter string
    - Build filter_graph exactly as in RESEARCH.md Pattern 9 (format=yuv420p + setpts on both inputs, model='path=...' syntax)
    - Run ffmpeg, drain proc iterator
    - Try json.load(log_path); try pooled_metrics path first; fallback frames average; fallback stderr regex
    - Always unlink log_path in finally block (log_path.unlink(missing_ok=True))
    - Raise PipelineError if vmaf is still None after all fallbacks

    Implement _encode_chunk_with_vmaf as async:
    - CRF starts at config["crf_start"]
    - visited_crfs: set[int] = set()
    - Loop up to 10 iterations:
      - _check_cancel(cancel_event)  — poll cancel between each iteration
      - await _encode_chunk_x264(ffmpeg, chunk_in, chunk_out, crf, config["x264_params"])
      - vmaf = await _vmaf_score(ffmpeg, chunk_out, ffv1_chunk, vmaf_model)
      - await append_job_log(db_path, job_id, f"  [{chunk_label}] iter {i}: CRF {crf} -> VMAF {vmaf:.2f}")
      - if vmaf_min <= vmaf <= vmaf_max: break
      - if crf in visited_crfs: break  (oscillation guard)
      - visited_crfs.add(crf)
      - if vmaf < vmaf_min and crf > crf_min: crf -= 1
      - elif vmaf > vmaf_max and crf < crf_max: crf += 1
      - else: break  (bounds hit)
    - print("[{chunk_label}] CRF {best_crf} -> VMAF {best_vmaf:.2f} ({status})")
    - await update_chunk(db_path, chunk_id=chunk_id, vmaf_score=best_vmaf, crf_used=best_crf, status="DONE")
    - return (best_crf, best_vmaf, len(visited_crfs) + 1)

    _check_cancel helper (already stubbed in Plan 01):
    ```python
    def _check_cancel(cancel_event):
        if cancel_event.is_set():
            raise PipelineError("Job cancelled", status="CANCELLED")
    ```
    Make PipelineError accept optional `status` kwarg and store as self.status (default "FAILED").
  </action>
  <verify>
    <automated>cd /c/VibeCoding/video-encoder && python -m pytest tests/test_pipeline.py::test_vmaf_score tests/test_pipeline.py::test_crf_feedback_loop tests/test_pipeline.py::test_crf_oscillation_guard -x -v 2>&1 | tail -15</automated>
  </verify>
  <done>3 tests GREEN: test_vmaf_score, test_crf_feedback_loop, test_crf_oscillation_guard. Running total: 11 GREEN of 14.</done>
</task>

</tasks>

<verification>
```
cd /c/VibeCoding/video-encoder && python -m pytest tests/test_pipeline.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR"
```
Must show 11 PASSED, 3 still FAILED (test_concat_mux, test_cleanup_on_success, test_cleanup_on_cancel — implemented in Plan 04).
</verification>

<success_criteria>
- 11 tests total GREEN (6 from Plan 02 + 5 new from this plan)
- VMAF filter graph uses model='path=...' syntax (not deprecated model_path=)
- format=yuv420p and setpts=PTS-STARTPTS applied to BOTH inputs in filter graph
- CRF loop terminates via oscillation guard (visited_crfs set) — not just iteration cap
- _check_cancel called at start of each CRF loop iteration
- VMAF temp log file always deleted (finally block)
</success_criteria>

<output>
After completion, create `.planning/phases/03-pipeline-runner/03-03-SUMMARY.md`
</output>
