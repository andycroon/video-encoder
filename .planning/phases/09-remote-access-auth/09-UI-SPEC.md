---
phase: 9
slug: remote-access-auth
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-18
---

# Phase 9 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — custom CSS variables + Tailwind 4 |
| Preset | not applicable |
| Component library | Radix UI primitives (`@radix-ui/react-dialog`, `@radix-ui/react-alert-dialog`, `@radix-ui/react-select`) |
| Icon library | Inline SVG (no external icon library — established pattern) |
| Font | 'Segoe UI Variable Display', 'Segoe UI', ui-sans-serif, system-ui (body) / 'Cascadia Code', 'JetBrains Mono' (mono) |

**Source:** `frontend/src/index.css` — full token set established in prior phases. Do not introduce new tokens.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Field gap within form row, compact spacing |
| md | 16px | Field label-to-input margin, form internal padding |
| lg | 24px | Card padding (matching TopBar panel at `padding: 20px`) |
| xl | 32px | Vertical gap between form sections |
| 2xl | 48px | Full-screen centering: vertical offset from center |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Form card max-width: 400px (login) / 440px (onboarding wizard)
- Form card border-radius: 12px (matches elevated card aesthetic)
- Input height: 42px (matches existing TopBar file input at `height: 42`)
- Button height: 38px for secondary / 42px for primary submit (matches existing Add Job button pattern)
- Touch target minimum: 44px for submit button on mobile viewport

**Source:** Existing inline styles in `TopBar.tsx`, `JobCard.tsx` — heights 38/42px, padding 20/28px established.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px | 400 | 1.5 |
| Label | 10px | 600 | 1 (uppercase, letter-spacing 0.1em) |
| Heading (wizard step title) | 16px | 600 | 1.2 |
| Display (page title: "VibeCoder Encoder") | 15px | 600 | 1 |

**Source:** `index.css` base font-size 14px / line-height 1.5. `TopBar.tsx` label style: `fontSize: 10, fontWeight: 600, letterSpacing: '0.1em', textTransform: 'uppercase'`. App header span: `fontWeight: 600, fontSize: 15`.

Mono copy (username/path display): 14px at weight 400 — use body size, no separate mono size declared.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--bg)` = `#0d0d0f` dark / `#1e1e22` light | Full-screen background behind auth pages |
| Secondary (30%) | `var(--panel)` = `#141417` / `var(--raised)` = `#1a1a1f` | Auth card surface / input field backgrounds |
| Accent (10%) | `var(--blue)` = `#4080ff` | Primary submit button (bg + border), input focus border glow |
| Destructive | `var(--red)` = `#ef4444` | Password mismatch error message, validation error text |

Accent reserved for:
- Submit button background (enabled state only: `background: #4080ff`)
- Submit button hover state: `background: #5590ff`
- Input focus border: `border-color: #4080ff80` (with opacity, matching existing TopBar file input hover)
- Disabled submit button: `background: #1a1a2e`, `border: 1px solid #2a2a45` (matching existing Add Job disabled state)

Border tokens:
- Card border: `1px solid var(--border)` = `#2a2a35`
- Input border: `1px solid var(--border)`
- Input focus: `1px solid #4080ff80`

Text tokens:
- Primary text: `var(--txt)` = `#f4f4f6`
- Secondary text: `var(--txt-2)` = `#d8d8e6` (input placeholder labels)
- Muted text: `var(--txt-3)` = `#a8a8bc` (helper text, step indicators)
- Error text: `var(--red)` = `#ef4444`
- Success text: `var(--green)` = `#22c55e` (account created confirmation)

**Source:** `index.css` CSS variable definitions. Color usage derived from `TopBar.tsx` Add Job button pattern.

---

## Component Inventory

This phase introduces two new full-screen page components and one auth store slice. No existing components are modified.

### New Components

| Component | Description |
|-----------|-------------|
| `LoginPage.tsx` | Full-screen centered card. Username + password fields. Submit button. No nav, no TopBar. |
| `OnboardingWizard.tsx` | Full-screen centered card. Single-step form: username + password + confirm password. Submit button. Progress indicator ("Set up your account"). |
| `ProtectedRoute.tsx` | Wrapper component that checks auth state and renders children or redirects to LoginPage / OnboardingWizard. |

### New Store Slice

| File | Fields |
|------|--------|
| `frontend/src/store/authStore.ts` | `token: string \| null`, `isAuthenticated: boolean`, `setToken(token: string): void`, `clearToken(): void` |

- Use individual Zustand selectors only: `useAuthStore(s => s.token)` — never inline object selectors.
- JWT persisted to `localStorage` under key `"vce_auth_token"`.

### New API Wrappers

| File | Exports |
|------|---------|
| `frontend/src/api/auth.ts` | `checkAuthStatus(): Promise<{ setup_required: boolean }>`, `login(username, password): Promise<{ access_token: string }>`, `register(username, password): Promise<{ access_token: string }>` |

### Existing API Layer Changes

| File | Change |
|------|--------|
| `frontend/src/api/jobs.ts` (and all api/*.ts) | Add `Authorization: Bearer <token>` header injection. On 401 response: call `clearToken()` from authStore and redirect to login. |

### App.tsx Change

Conditional rendering at root level (no react-router dependency):
1. `GET /api/auth/status` on mount.
2. If `setup_required: true` → render `<OnboardingWizard />` full-screen.
3. If `setup_required: false` and `!isAuthenticated` → render `<LoginPage />` full-screen.
4. If `isAuthenticated` → render existing `<App />` content (TopBar + JobList).

---

## Page Layouts

### LoginPage Layout

```
┌─────────────────────────────────────────┐
│              (full-screen)              │
│                                         │
│  ┌───────────────── 400px ────────────┐ │
│  │  [logo svg]  VibeCoder  Encoder    │ │  ← centered logo row, matches App header
│  │                                    │ │
│  │  ─────────────────────────────     │ │  ← 1px border-lo divider
│  │                                    │ │
│  │  USERNAME                          │ │  ← 10px label, uppercase
│  │  [___________________________]     │ │  ← 42px input
│  │                                    │ │
│  │  PASSWORD                          │ │  ← 10px label
│  │  [___________________________]     │ │  ← 42px input type=password
│  │                                    │ │
│  │  [      Sign In          ]         │ │  ← 42px full-width, blue accent
│  │                                    │ │
│  │  {error message if any}            │ │  ← red, 12px, below button
│  └────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

- Card: `background: var(--panel)`, `border: 1px solid var(--border)`, `border-radius: 12px`, `padding: 28px`
- Full-screen wrapper: `min-height: 100vh`, `display: flex`, `align-items: center`, `justify-content: center`, `background: var(--bg)`
- No "forgot password" link — single user, reset via SQLite CLI documented in README
- Primary focal point: the "Sign In" button — sole use of accent blue on the screen draws the eye after the credential fields.

### OnboardingWizard Layout

```
┌─────────────────────────────────────────┐
│              (full-screen)              │
│                                         │
│  ┌───────────────── 440px ────────────┐ │
│  │  [logo svg]  VibeCoder  Encoder    │ │
│  │                                    │ │
│  │  Set up your account               │ │  ← 16px, weight 600, var(--txt)
│  │  Create credentials to protect     │ │  ← 14px, var(--txt-3)
│  │  remote access to this instance.   │ │
│  │                                    │ │
│  │  ─────────────────────────────     │ │
│  │                                    │ │
│  │  USERNAME                          │ │
│  │  [___________________________]     │ │  ← 42px input
│  │                                    │ │
│  │  PASSWORD                          │ │
│  │  [___________________________]     │ │  ← 42px input type=password
│  │                                    │ │
│  │  CONFIRM PASSWORD                  │ │
│  │  [___________________________]     │ │  ← 42px input type=password
│  │                                    │ │
│  │  {validation message if any}       │ │  ← red, 12px
│  │                                    │ │
│  │  [      Create Account       ]     │ │  ← 42px full-width, blue accent
│  └────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

- Same card styling as LoginPage
- No multi-step wizard navigation needed — content is minimal (3 fields), single step is sufficient
- "Wizard feel" satisfied by dedicated full-screen layout + descriptive subheading copy

---

## Interaction States

### Input Fields

| State | Visual |
|-------|--------|
| Default | `border: 1px solid var(--border)`, `background: var(--raised)` |
| Focus | `border: 1px solid #4080ff80`, outline: none |
| Error (validation) | `border: 1px solid var(--red)` |
| Disabled (submitting) | `opacity: 0.5`, `cursor: not-allowed` |

### Submit Button

| State | Visual |
|-------|--------|
| Enabled | `background: #4080ff`, `border: 1px solid #4080ff`, `color: white` |
| Hover | `background: #5590ff` |
| Disabled (empty fields) | `background: #1a1a2e`, `border: 1px solid #2a2a45`, `color: var(--txt-3)`, `cursor: not-allowed` |
| Loading (request in flight) | Button text → "Signing in…" / "Creating account…", disabled |

### Error Display

- Position: below the submit button, `margin-top: 8px`
- Style: `fontSize: 12px`, `color: var(--red)`, no border/background — plain text
- Animation: none — appears/disappears immediately on state change

### Success (onboarding complete)

- No separate confirmation page — JWT received → token stored → immediate redirect to main app
- Main app renders normally; no toast notification required

---

## Auth Routing Logic

```
App mounts
  ↓
GET /api/auth/status
  ├─ { setup_required: true }  → render <OnboardingWizard /> (full-screen, replaces app)
  ├─ { setup_required: false } + no token → render <LoginPage /> (full-screen, replaces app)
  └─ { setup_required: false } + valid token → render existing app content
```

Loading state during `GET /api/auth/status`:
- Show full-screen `var(--bg)` background only — no spinner, no skeleton
- Transition is fast (local DB check); flicker prevention not required

401 interception in API layer:
- Clear `localStorage["vce_auth_token"]`
- Call `clearToken()` in authStore
- App re-renders → `!isAuthenticated` condition → LoginPage appears

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (login) | "Sign In" |
| Primary CTA (onboarding) | "Create Account" |
| Loading state (login) | "Signing in…" |
| Loading state (onboarding) | "Creating account…" |
| Onboarding heading | "Set up your account" |
| Onboarding subheading | "Create credentials to protect remote access to this instance." |
| Empty username error | "Username is required." |
| Empty password error (login) | "Password is required." |
| Wrong credentials error | "Invalid username or password." |
| Empty password error (onboarding) | "Password is required." |
| Password too short error | "Password must be at least 8 characters." |
| Password mismatch error | "Passwords do not match." |
| 401 session expired | LoginPage re-renders — no toast copy needed |

Destructive actions in this phase: none. No delete/clear actions on auth pages.

Logo area: identical to App header — `[svg] VibeCoder` (weight 600, 15px) + `Encoder` (uppercase, 11px, `var(--txt-3)`). Reuse App header logo markup verbatim.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| third-party | none | not applicable |

No third-party registries. All components are hand-authored following the established pattern (inline styles + Radix Dialog if needed for future modal, but auth pages are full-screen renders, not dialogs).

---

## Accessibility

- All inputs have visible `<label>` elements (or `aria-label` if no visible label is used)
- Error messages use `role="alert"` so screen readers announce them immediately
- Submit button `disabled` attribute set (not just `cursor: not-allowed`) when fields are empty or request is in flight
- Password inputs: `type="password"` with `autoComplete="current-password"` (login) / `autoComplete="new-password"` (onboarding)
- Username input: `autoComplete="username"`
- Form submits on Enter keypress in any field (standard `<form onSubmit>` with `type="submit"` button)

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
