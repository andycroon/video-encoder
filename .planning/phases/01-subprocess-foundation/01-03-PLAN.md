---
phase: 01-subprocess-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - README.md
autonomous: true
requirements:
  - PIPE-10
must_haves:
  truths:
    - "README.md exists at the project root"
    - "A reader can determine the required Python version, ffmpeg install path, and how to install scenedetect from README.md"
    - "A reader can determine where to place VMAF model files (assets/ directory) from README.md"
    - "A reader can determine how to install the encoder package and run tests from README.md"
  artifacts:
    - path: "README.md"
      provides: "Phase 1 documentation section — prerequisites, VMAF setup, dev setup"
      contains: "Python 3.9"
  key_links:
    - from: "README.md"
      to: "assets/"
      via: "VMAF model setup instructions"
      pattern: "assets/"
    - from: "README.md"
      to: "pyproject.toml"
      via: "pip install -e .[dev] install command"
      pattern: "pip install"
---

<objective>
Write the Phase 1 section of README.md — system prerequisites, VMAF model setup, and dev environment setup. This satisfies Phase 1 Success Criterion 5 from the Roadmap.

Purpose: README is built incrementally per the project strategy (DOC-01). Phase 1 owns the installation and prerequisites section. Later phases append their own sections.
Output: README.md with a complete Phase 1 documentation section.
</objective>

<execution_context>
@C:/Users/owner/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/owner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-subprocess-foundation/01-CONTEXT.md
@.planning/phases/01-subprocess-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md with Phase 1 prerequisites section</name>
  <files>README.md</files>
  <action>
    Create README.md at C:/VibeCoding/video-encoder/README.md.

    The README must cover these topics in this order:

    1. Project title and one-sentence description:
       "VibeCoder Video Encoder — batch x264 encoding with VMAF-targeted quality, designed for a single-user local workflow."

    2. System Prerequisites section with these subsections:
       a. Python version: 3.9 or higher. Provide install link: https://www.python.org/downloads/
       b. ffmpeg:
          - Windows: place ffmpeg.exe at C:\ffmpeg\ffmpeg.exe (the encoder expects this path)
            Download from https://ffmpeg.org/download.html — use a build with libx264 and libvmaf enabled
          - Linux: install via package manager (e.g., `sudo apt install ffmpeg`) or compile from source
            with --enable-libx264 --enable-libvmaf
          - Verify: `ffmpeg -version` should show libx264 and libvmaf in configuration
       c. PySceneDetect:
          - `pip install "scenedetect[opencv]>=0.6.7,<0.7"`
          - Verify: `scenedetect version`
       d. Note: Plex Transcoder (EAC3 audio) is only required if using the EAC3 audio codec option
          (Phase 3 feature). Not required for Phase 1 or 2.

    3. VMAF Model Setup section:
       - The encoder expects VMAF model files in the `assets/` directory at the project root
       - Model file needed: `vmaf_v0.6.1.json` (standard HD model)
       - Download from: https://github.com/Netflix/vmaf/tree/master/model
       - Place at: `assets/vmaf_v0.6.1.json`
       - Note: The assets/ directory already exists in this repo with the model file included.
         No action required if cloning this repository.

    4. Development Setup section:
       ```bash
       # Clone the repo
       git clone <repo-url>
       cd video-encoder

       # Install the encoder package in editable mode + dev dependencies
       pip install -e ".[dev]"

       # Run the test suite
       pytest tests/ -v
       ```
       Note: Tests use real ffmpeg (lavfi synthetic sources). Ensure ffmpeg is on PATH
       (or at C:\ffmpeg\ffmpeg.exe on Windows) before running tests.
       Expected runtime: ~20 seconds.

    5. Brief "What This Is" section explaining this is currently a Python library/CLI
       (phases 1–3) that will gain a web UI in Phase 5. Link to roadmap for full context.

    Use clear Markdown headings (##, ###). No emojis. Keep it concise — this is
    developer documentation, not marketing copy. Later phases will append sections below.
  </action>
  <verify>
    <automated>python -c "import pathlib; content = pathlib.Path('README.md').read_text(); assert 'Python 3.9' in content; assert 'assets/' in content; assert 'pip install' in content; print('README.md content verified')"</automated>
  </verify>
  <done>README.md exists containing: Python 3.9 prerequisite, ffmpeg install path for Windows (C:\ffmpeg\ffmpeg.exe), scenedetect install command, assets/vmaf_v0.6.1.json setup, and pip install -e ".[dev]" dev setup command</done>
</task>

</tasks>

<verification>
Manual check: Open README.md and verify it reads clearly as a getting-started document.
A new developer should be able to follow it to get tests passing on Windows or Linux
without any prior context about the project.

Check all five areas are covered:
  1. Python version requirement visible
  2. ffmpeg Windows path (C:\ffmpeg\ffmpeg.exe) explicitly stated
  3. scenedetect install command present
  4. assets/vmaf_v0.6.1.json location documented
  5. pip install -e ".[dev]" + pytest command present
</verification>

<success_criteria>
- README.md exists at project root
- Contains all five required content areas listed above
- Readable and accurate — no placeholder text
- Correctly documents C:\ffmpeg\ffmpeg.exe as the Windows ffmpeg path
</success_criteria>

<output>
After completion, create `.planning/phases/01-subprocess-foundation/01-03-SUMMARY.md`
</output>
